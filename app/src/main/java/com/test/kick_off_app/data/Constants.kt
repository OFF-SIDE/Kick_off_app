package com.test.kick_off_app.data

object Constants {
    val CATEGORY_SIZE = CategoryEnum.values().size
    val LOCATION_SIZE = LocationEnum.values().size
    val STATUS_SIZE = StatusEnum.values().size
}